{ 
    "script_name": "trns_transform_KBaseBiochem.Media-to-CSV.pl", 
    "script_type": "download", 
    "external_type": "CSV", 
    "kbase_type": "KBaseBiochem.Media", 
    "user_description": "Download media in CSV format", 
    "developer_description": "Download media in CSV format", 
    "handler_options": { 
        "max_runtime": 600, 
        "required_fields": [ 
            "input_file_name",
            "workspace_name", 
        ], 
        "optional_fields": ["version","url"], 
        "custom_options": [], 
    },  
    "user_options": [ 
        { 
            "name": "input_file_name", 
            "type": "string", 
            "required": true, 
            "help": "Name of the workspace object." 
        }, 
        { 
            "name": "workspace_name",
            "type": "string", 
            "required": false,
            "help": "Workspace containing object."
        }, 
        { 
            "name": "version", 
            "type": "int", 
            "required": false,
            "help": "Version of the workspace object, if not given we assume the latest."
        } 
    ], 
    "user_option_groups": [] 
}
