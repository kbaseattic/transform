{ 
    "script_name": "trns_transform_KBasePhenotypes.PhenotypeSimulationSet-to-CSV.pl", 
    "script_type": "download", 
    "external_type": "CSV", 
    "kbase_type": "KBasePhenotypes.PhenotypeSimulationSet", 
    "user_description": "Download phenotype simulation set in CSV format", 
    "developer_description": "Download phenotype simulation set in CSV format", 
    "handler_options": { 
        "max_runtime": 600, 
        "required_fields": [ 
            "input",
            "workspace", 
        ], 
        "optional_fields": ["version","url","output"], 
        "custom_options": [], 
    },  
    "user_options": [ 
        { 
            "name": "input", 
            "type": "string", 
            "required": true, 
            "help": "Name of the workspace object." 
        }, 
        { 
            "name": "workspace",
            "type": "string", 
            "required": false,
            "help": "Workspace containing object."
        }, 
        { 
            "name": "version", 
            "type": "int", 
            "required": false,
            "help": "Version of the workspace object, if not given we assume the latest."
        } 
    ], 
    "user_option_groups": [] 
}
